# Agent Instructions

1. Preserve the six-surface authority matrix in `CANON.md` and `ARTIFACT.json`.
2. Never describe CAP as a global certification or merged specification.
3. Keep source pins exact and synchronized across `ARTIFACT.json`, `references/`, examples, fixtures, and validator constants.
4. Preserve native source statuses as opaque owner-defined tokens. CAP may map them only through an explicit `mapping_rule`.
5. Do not make the reference validator normative.
6. Do not convert ARB into a normative source.
7. Any new result status, disposition, or precedence rule requires a versioned normative change and conformance cases.
8. Run the canonical checks before publication.
